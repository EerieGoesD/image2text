<Window x:Class="Image2Text.ThemedDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title=""
        Icon="pack://application:,,,/icon.ico"
        Width="460" SizeToContent="Height"
        MinHeight="180"
        Background="{StaticResource BgBrush}"
        Foreground="{StaticResource TextBrush}"
        FontFamily="{StaticResource SansFont}"
        FontSize="13"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">

    <Grid Margin="24,20,24,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title + accent icon -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock x:Name="IconText" Text="ⓘ" FontSize="20" FontWeight="Bold"
                       Foreground="{StaticResource AccentHBrush}" VerticalAlignment="Center"
                       Margin="0,0,10,0"/>
            <TextBlock x:Name="HeaderText" FontSize="15" FontWeight="Bold"
                       Foreground="{StaticResource TextBrush}" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Message -->
        <TextBlock Grid.Row="1" x:Name="MessageText" TextWrapping="Wrap"
                   Foreground="{StaticResource TextBrush}" Margin="0,0,0,20"
                   LineHeight="20"/>

        <!-- Actions -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="CancelButton" Content="Cancel" Style="{StaticResource GhostButton}"
                    Margin="0,0,8,0" MinWidth="90" Click="Cancel_Click" Visibility="Collapsed"/>
            <Button x:Name="PrimaryButton" Content="OK" Style="{StaticResource AccentButton}"
                    MinWidth="90" Click="Primary_Click" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
